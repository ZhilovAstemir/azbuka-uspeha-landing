# Азбука успеха — лендинг детского центра развития (Нальчик)

Одностраничный сайт на **Next.js 16 + React 19 + TypeScript + Tailwind CSS v4**
с анимациями (Framer Motion), SEO-разметкой и подключением аналитики.

Ветка `redesign/pro` — профессиональная версия дизайна: шрифты Unbounded + Onest,
линейные SVG-иконки вместо эмодзи, сдержанная сетка, палитра логотипа
(тёмно-синий текст, красный акцент, жёлтый/зелёный/голубой для деталей).

Контент заполнен по данным Instagram **@azbuka_yspeha** (выгрузка от 19.09.2026):
адрес, телефон, направления, продлёнка, акция −20%, два преподавателя, фото.

## Быстрый старт

```bash
npm install       # установить зависимости
npm run dev       # локально: http://localhost:3000
npm run build     # production-сборка
npm run start     # запустить собранную версию
```

## Где менять контент

Весь текст, направления, педагоги, отзывы, форматы, FAQ и контакты — в одном файле:

> **`src/lib/content.ts`**

Поля, помеченные `TODO`, в Instagram отсутствовали — их нужно заполнить.

## Что ещё нужно от вас

- [ ] **Цены.** В Instagram не публиковались. Впишите в `formats[].price` (сейчас «по запросу»).
- [ ] **Домен** — `site.url` (нужен для SEO, Open Graph, sitemap).
- [ ] **Часы работы и email**, если хотите показать их на сайте.
- [ ] **ID Яндекс.Метрики и GA4** — `site.analytics`. Пока пустые, счётчики не грузятся.
- [ ] **Приём заявок.** Форма сейчас имитирует отправку (см. `TODO` в `src/components/LeadForm.tsx`).
      Варианты: API-роут `src/app/api/lead/route.ts` → Telegram-бот / почта / CRM, либо сервис форм.
- [ ] **Видео.** Ссылки на mp4 из выгрузки уже недействительны (403). Если пришлёте файлы
      видео продлёнки — добавлю автоплей-видео в блок продлёнки, это сильно повышает доверие.
- [ ] **Фото педагогов без надписей.** Сейчас используются обложки из Instagram с текстовой плашкой
      снизу (обрезана). Чистые портреты будут смотреться лучше.
- [ ] **Логотип в высоком разрешении.** Сейчас аватар 150×150 из Instagram (`public/images/avatar.jpg`).
- [ ] **Политика конфиденциальности** уже есть (`/privacy`, ИП Индрисова О. Б.) — стоит показать юристу.

## Структура

```
src/
├─ app/
│  ├─ layout.tsx        # шрифты, SEO-метаданные, JSON-LD (ChildCare + адрес в Нальчике), аналитика
│  ├─ page.tsx          # сборка секций
│  ├─ globals.css       # дизайн-система
│  ├─ icon.jpg          # favicon (логотип центра)
│  ├─ privacy/page.tsx  # политика конфиденциальности
│  ├─ robots.ts / sitemap.ts
├─ components/
│  ├─ HeroMedia.tsx     # фото-блок героя с карточками и параллаксом
│  ├─ Icon.tsx          # набор SVG-иконок
│  ├─ Header.tsx, LeadForm.tsx, Analytics.tsx …
│  └─ sections/         # Hero, Programs (с продлёнкой), About, Teachers, Reviews,
│                       # Prices (форматы), Faq, Contacts, Footer
├─ lib/content.ts       # ← ВЕСЬ РЕДАКТИРУЕМЫЙ КОНТЕНТ
public/
├─ images/              # фото из Instagram (12 файлов) + аватар-логотип
└─ og-image.jpg         # картинка для соцсетей 1200×630
```

## Аналитика

`src/lib/content.ts` → `site.analytics`:

```ts
analytics: {
  yandexMetrikaId: "12345678",     // номер счётчика Яндекс.Метрики
  gaMeasurementId: "G-XXXXXXXXXX", // ID Google Analytics 4
}
```

При отправке формы уходит цель `lead` в Метрику и событие `generate_lead` в GA4.

## Деплой

Без изменений разворачивается на **Vercel** (рекомендуется для Next.js) или на любом
хостинге с Node.js: `npm run build && npm run start`.

## Анимации

- Плавное появление блоков при прокрутке, параллакс фото в герое.
- Уважает `prefers-reduced-motion`.
